import { AuthOptions } from '../types';

export const testConfig: AuthOptions = {
  appId: 'your-app-id',
  appSecret: 'your-app-secret',
  permanentCode: 'your-permanent-code',
  // 可选参数
  phone: 'your-phone-number'
}; 